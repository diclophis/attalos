#!/bin/sh

set -x

#if [ ! -z $BROWSERIFY ]; then
  ruby -r 'rubygems' -r 'closure-compiler' -e "puts Closure::Compiler.new(:warning_level => 'QUIET', :compilation_level => 'SIMPLE_OPTIMIZATIONS').compile(STDIN)" < $2 > $3
#else
#  #./node_modules/.bin/webpack -d
#  #./node_modules/.bin/webpack --output-source-map-file --output-library $1 --output-library-target umd $2 $3
#  true
#fi
